/// @description Insert description here
// You can write your code in this editor

var inst = instance_find(objRightArrow, 0);
with(inst)
{
	inst.isEnabled = true;
}
var inst = instance_find(objLeftArrow, 0);
with(inst)
{
	inst.isEnabled = true;
}